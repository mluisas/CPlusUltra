// C++ Template

#include <bits/stdc++.h>

using namespace std;

int main() {
    // Make input and output more efficient
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int a, b;
    // Default input and output
    cin >> a >> b;
    cout << a << " + " << b << " = " << a + b << "\n";
}

// To compile
// g++ -std=c++14 -O2 -Wall code.cpp -o bin







